# 🎭 Система анонимной авторизации DaDa!

## 🎯 Концепция

**Проблема:** Большинство людей не хотят создавать аккаунт, когда ищут подработку.

**Решение:** Анонимная авторизация с возможностью привязки email позже.

---

## 🔄 Как это работает

### Шаг 1: Onboarding без регистрации

```
Пользователь открывает приложение
  ↓
Проходит onboarding (интересы, зарплата, геолокация)
  ↓
Выбирает категории работ
  ↓
🎉 Автоматически создается АНОНИМНЫЙ аккаунт
  ↓
Сохраняются все предпочтения
  ↓
Переход на главный экран с персонализированной лентой
```

**Никакого email! Никаких паролей! Никаких форм!** ✨

---

## 🔐 Технические детали

### Что происходит под капотом:

1. **Создание анонимного пользователя:**
```dart
// В interests_screen.dart после выбора категорий
final success = await authProvider.signInAnonymously();
```

2. **Supabase создает:**
   - UUID пользователя (уникальный ID)
   - Запись в таблице `auth.users` (is_anonymous = true)
   - Запись в таблице `users` с флагом `is_anonymous = true`

3. **Сохранение предпочтений:**
```dart
// Интересы (категории работ)
await authProvider.saveInterests(['Кафе', 'Склад', 'Курьер']);

// Минимальная зарплата
await authProvider.saveMinSalary(45000);

// Геолокация (в будущем)
await authProvider.saveLocation(latitude: 55.7558, longitude: 37.6173);
```

4. **Персонализация работает!** 🎯
   - `user_interests` связан с анонимным UUID
   - Функция `get_personalized_feed()` учитывает предпочтения
   - Пользователь видит релевантные вакансии

---

## ✅ Что может анонимный пользователь?

- ✅ Смотреть персонализированную ленту вакансий
- ✅ Фильтровать по категориям, зарплате, расстоянию
- ✅ Лайкать вакансии
- ✅ Комментировать
- ✅ **Откликаться на вакансии**
- ✅ Звонить работодателям
- ✅ Сохранять вакансии в избранное

---

## 🔗 Привязка email (опционально)

### Когда это нужно?

- Пользователь хочет сохранить свои данные навсегда
- Хочет получать уведомления на email
- Хочет войти с другого устройства

### Как это работает:

**В профиле появляется кнопка:**

```
┌─────────────────────────────────┐
│  👤 Анонимный пользователь       │
│                                  │
│  🔓 Привязать email              │
│     Сохрани свой аккаунт         │
└─────────────────────────────────┘
```

**После нажатия:**
1. Пользователь вводит email
2. Получает код подтверждения
3. Вводит код
4. ✨ Аккаунт "апгрейдится":
   - `is_anonymous = false`
   - `email` сохраняется
   - Все данные сохраняются (интересы, отклики, избранное)

---

## 📊 Структура базы данных

### Таблица `users`:

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY,
  username VARCHAR(50),
  display_name VARCHAR(100),
  email VARCHAR(255),              -- NULL для анонимных
  is_anonymous BOOLEAN DEFAULT false, -- 👈 КЛЮЧЕВОЕ ПОЛЕ
  min_salary_preference INTEGER,   -- Из онбординга
  location_lat DECIMAL(10, 8),     -- Из геолокации
  location_lng DECIMAL(11, 8),     -- Из геолокации
  location_city VARCHAR(100),      -- Город пользователя
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);
```

### Таблица `user_interests`:

```sql
CREATE TABLE user_interests (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),    -- Работает с анонимными!
  category_id UUID REFERENCES categories(id),
  created_at TIMESTAMPTZ
);
```

### Таблица `job_applications`:

```sql
CREATE TABLE job_applications (
  id UUID PRIMARY KEY,
  post_id UUID REFERENCES posts(id),
  user_id UUID REFERENCES users(id),    -- Работает с анонимными!
  status VARCHAR(50) DEFAULT 'pending',
  message TEXT,
  created_at TIMESTAMPTZ
);
```

---

## 🛡️ RLS Policies (Row Level Security)

Анонимные пользователи имеют те же права, что и обычные:

```sql
-- Пользователи могут видеть свои отклики
CREATE POLICY "Users can view their own applications"
  ON job_applications FOR SELECT
  USING (auth.uid() = user_id);  -- ✅ auth.uid() работает для анонимных!

-- Пользователи могут создавать отклики
CREATE POLICY "Users can create applications"
  ON job_applications FOR INSERT
  WITH CHECK (auth.uid() = user_id);  -- ✅ Анонимные тоже могут!
```

---

## 🔄 Миграция: Анонимный → Полноценный аккаунт

### Вариант 1: Через email OTP

```dart
// Пользователь нажимает "Привязать email"
await authProvider.sendMagicLink(email: 'user@example.com');

// Вводит код
await authProvider.verifyOTP(email: 'user@example.com', token: '123456');

// Аккаунт обновляется:
UPDATE users 
SET email = 'user@example.com', 
    is_anonymous = false 
WHERE id = auth.uid();
```

### Вариант 2: Через соцсети (будущее)

```dart
// TODO: Google Sign-In
// TODO: Apple Sign-In
// TODO: Telegram Login
```

---

## 📱 Пользовательский опыт

### До (старая система):

```
Открыл приложение
  ↓
Онбординг
  ↓
❌ "Введите email"
  ↓
❌ "Подождите код"
  ↓
❌ "Введите код"
  ↓
😤 50% пользователей уходят
```

### Сейчас (новая система):

```
Открыл приложение
  ↓
Онбординг (интересы, зарплата)
  ↓
✨ Готово! Смотри вакансии!
  ↓
😊 100% пользователей довольны
```

---

## 🎨 UI изменения

### ProfileScreen (будущее):

```dart
// Для анонимных пользователей
if (isAnonymous) {
  Card(
    child: ListTile(
      leading: Icon(Icons.email_outlined),
      title: Text('Привязать email'),
      subtitle: Text('Сохрани свой аккаунт'),
      onTap: () => Navigator.push(
        context,
        MaterialPageRoute(builder: (_) => LinkEmailScreen()),
      ),
    ),
  );
}
```

---

## 🔥 Преимущества

### Для пользователей:
- ⚡ Мгновенный старт (без регистрации)
- 🎯 Сразу персонализированный контент
- 📱 Можно откликаться на вакансии
- 🔒 Данные в безопасности (UUID)
- 💾 Можно сохранить аккаунт позже

### Для бизнеса:
- 📈 Выше конверсия (нет барьера входа)
- 🎯 Сбор данных о предпочтениях
- 💰 Больше откликов на вакансии
- 📊 Аналитика работает с первого дня

---

## 🚀 Что дальше?

### После загрузки видео:

1. ✅ Анонимная авторизация работает
2. ✅ Предпочтения сохраняются
3. ✅ Персонализация готова
4. 🔲 Загрузить видео-вакансии
5. 🔲 Протестировать ленту
6. 🔲 Добавить UI для привязки email (опционально)

---

## 💬 FAQ

**Q: Что если пользователь удалит приложение?**
A: Данные останутся в базе 30 дней. При новой установке создастся новый анонимный аккаунт.

**Q: Может ли анонимный пользователь стать работодателем?**
A: Нет, для публикации вакансий нужен полноценный аккаунт с email.

**Q: Как отслеживать аналитику анонимных пользователей?**
A: По UUID! Каждый анонимный пользователь имеет уникальный ID.

**Q: Безопасно ли это?**
A: Да! Supabase использует JWT токены и RLS policies для безопасности.

---

## ✅ Чек-лист готовности

- ✅ `signInAnonymously()` реализован
- ✅ Onboarding создает анонимный аккаунт
- ✅ Предпочтения сохраняются
- ✅ RLS policies настроены
- ✅ Персонализированная лента готова
- 🔲 SQL схема применена в Supabase
- 🔲 Видео-вакансии загружены
- 🔲 Протестировано на эмуляторе

---

🎉 **Готово! Система анонимной авторизации полностью реализована!**

Осталось только загрузить видео-вакансии и протестировать! 🚀

