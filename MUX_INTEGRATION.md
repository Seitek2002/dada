# 🎥 Mux Video Integration Guide

## ✅ Что реализовано

### 1. **MuxVideoPlayerWidget** - Новый видеоплеер
- ✅ Поддержка **Mux HLS** (.m3u8)
- ✅ Поддержка **локальных видео** (assets)
- ✅ Поддержка **обычных MP4** 
- ✅ **Масштабирование двумя пальцами** (pinch to zoom)
- ✅ **Автоматическое скрытие UI** при зуме

### 2. **Функционал масштабирования**
- Увеличьте видео двумя пальцами
- UI элементы автоматически скрываются:
  - ❌ Кнопки справа (like, comment, share)
  - ❌ Описание видео снизу
  - ❌ Градиент overlay
- Видео увеличивается плавно
- Отпустите пальцы - UI возвращается

### 3. **BetterPlayer** вместо video_player
- Более стабильная работа
- Поддержка HLS (формат Mux)
- Лучшая производительность
- Нет ошибок аудио синхронизации

---

## 🎬 Как работает масштабирование

### Пользователь:
1. **Двумя пальцами** увеличивает видео
2. **UI автоматически скрывается**
3. Видео занимает весь экран
4. **Отпускает пальцы** → UI возвращается

### Технически:
```dart
onScaleUpdate: (details) {
  if (details.scale > 1.2) {
    // Скрыть UI
    setState(() => _isZoomed = true);
  }
}
```

---

## 🔗 Интеграция с Mux

### Формат Mux URL:
```
https://stream.mux.com/{PLAYBACK_ID}.m3u8
```

### Пример в коде:
```dart
MuxVideoPlayerWidget(
  videoUrl: 'https://stream.mux.com/YOUR_PLAYBACK_ID.m3u8',
  isPlaying: true,
  onZoomChanged: (isZoomed) {
    print('Zoomed: $isZoomed');
  },
)
```

---

## 📝 Типы поддерживаемых URL

### 1. **Mux HLS Stream** (.m3u8)
```dart
videoUrl: 'https://stream.mux.com/abc123.m3u8'
```

### 2. **Локальные assets**
```dart
videoUrl: 'assets/videos/video1.mp4'
```

### 3. **Обычные MP4**
```dart
videoUrl: 'https://example.com/video.mp4'
```

---

## 🎨 UI Поведение

### Обычный режим (без зума):
```
┌─────────────────┐
│                 │
│     VIDEO       │
│                 │
│   ┌─────────┐   │
│   │Description│  │ ← Видно
│   └─────────┘   │
│                 │
│    [Like]       │ ← Видно
│    [Comment]    │ ← Видно
│    [Share]      │ ← Видно
└─────────────────┘
```

### Режим зума (pinch to zoom):
```
┌─────────────────┐
│                 │
│                 │
│   V I D E O     │
│   (ZOOMED)      │
│                 │
│                 │
│                 │ ← UI скрыт
│                 │ ← UI скрыт
│                 │ ← UI скрыт
└─────────────────┘
```

---

## 🚀 Как использовать локально (пока без Mux)

### Текущие видео работают:
```dart
// Локальные видео автоматически подхватываются
MuxVideoPlayerWidget(
  videoUrl: 'assets/videos/video1.mp4',
  isPlaying: true,
)
```

### Когда добавите Mux:
1. Загрузите видео в Mux
2. Получите `PLAYBACK_ID`
3. Замените URL:
```dart
videoUrl: 'https://stream.mux.com/YOUR_PLAYBACK_ID.m3u8'
```

---

## 📊 Структура Supabase для Mux

### Обновите поле `media_url` в таблице `posts`:

```sql
-- Добавьте колонку для Mux Playback ID
ALTER TABLE posts 
ADD COLUMN mux_playback_id TEXT;

-- Добавьте колонку для Mux Asset ID
ALTER TABLE posts 
ADD COLUMN mux_asset_id TEXT;

-- Функция для получения полного Mux URL
CREATE OR REPLACE FUNCTION get_mux_url(playback_id TEXT)
RETURNS TEXT AS $$
BEGIN
  RETURN 'https://stream.mux.com/' || playback_id || '.m3u8';
END;
$$ LANGUAGE plpgsql;
```

### Использование в приложении:
```dart
final muxUrl = 'https://stream.mux.com/${post.muxPlaybackId}.m3u8';
```

---

## 🎯 Workflow загрузки видео в Mux

### 1. Пользователь выбирает видео
```dart
final video = await ImagePicker().pickVideo();
```

### 2. Загрузка в Supabase Storage
```dart
final path = await supabase.storage
  .from('videos')
  .upload('temp/${uuid}.mp4', video);
```

### 3. Создание Mux Asset через API
```dart
POST https://api.mux.com/video/v1/assets
{
  "input": "https://YOUR_SUPABASE_URL/storage/v1/object/public/videos/temp/uuid.mp4",
  "playback_policy": ["public"]
}
```

### 4. Сохранение Mux ID в Supabase
```dart
await supabase.from('posts').insert({
  'mux_asset_id': response['data']['id'],
  'mux_playback_id': response['data']['playback_ids'][0]['id'],
  'media_url': 'https://stream.mux.com/${playbackId}.m3u8',
});
```

---

## 🔧 Настройки BetterPlayer

### В `mux_video_player_widget.dart`:
```dart
BetterPlayerConfiguration(
  autoPlay: true,
  looping: false,
  aspectRatio: 9 / 16,  // TikTok формат
  fit: BoxFit.contain,   // Не растягивать
  controlsConfiguration: BetterPlayerControlsConfiguration(
    showControls: false, // Все контролы скрыты
  ),
)
```

---

## 📱 Тестирование

### Локально:
```bash
flutter run -d emulator-5554
```

### С Mux (тестовый URL):
Используйте тестовый Mux stream:
```dart
videoUrl: 'https://stream.mux.com/DS00Spx1CV902MCtPj5WknGlR102V5HFkDe.m3u8'
```

---

## 🎉 Преимущества

### BetterPlayer:
- ✅ Нет ошибок аудио синхронизации
- ✅ Поддержка HLS из коробки
- ✅ Лучшая производительность
- ✅ Встроенная обработка ошибок

### Масштабирование:
- ✅ Нативные жесты (pinch)
- ✅ Плавная анимация
- ✅ Автоматическое управление UI
- ✅ Интуитивно для пользователей

### Mux:
- ✅ Адаптивный битрейт
- ✅ Оптимизация для мобильных сетей
- ✅ Аналитика просмотров
- ✅ Автоматическое транскодирование

---

## 🔜 Следующие шаги

1. **Получить Mux API ключи**:
   - https://dashboard.mux.com/
   - Settings → Access Tokens

2. **Настроить загрузку видео**:
   - Создать экран загрузки
   - Интегрировать Mux API
   - Обновить Supabase схему

3. **Добавить аналитику**:
   - Отслеживание просмотров через Mux
   - Данные о качестве воспроизведения
   - Географическая статистика

---

## 📞 Полезные ссылки

- [Mux Documentation](https://docs.mux.com/)
- [BetterPlayer Package](https://pub.dev/packages/better_player)
- [Flutter Gestures](https://api.flutter.dev/flutter/widgets/GestureDetector-class.html)

---

Готово! 🚀 Теперь ваш TikTok клон поддерживает:
- ✅ Mux Video Streaming
- ✅ Pinch to Zoom
- ✅ Автоматическое скрытие UI
- ✅ Плавную работу без ошибок

